#include "competition/opcontrol.h"
#include "vex.h"

/**
 * Main entrypoint for the driver control period
*/
void opcontrol()
{
    // ================ INIT ================
	asdf

    // ================ PERIODIC ================
    while(true)
    {

        vexDelay(10);
    }
}
